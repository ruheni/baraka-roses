/**
 *  GET - orders
 *  POST - create || update order's data
 *  GET/:id - fetch order's data
 */
const orders_get = (req, res) => {
    res.json({ message: 'coming soon' })
}

const order_details_get = (req, res) => {
    res.json({ message: 'coming soon' })
}

const order_details_post = (req, res) => {
    res.json({ message: 'coming soon' })
}

export {
    order_details_get,
    order_details_post,
    orders_get
}
